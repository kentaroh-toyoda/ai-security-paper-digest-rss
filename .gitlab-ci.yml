stages:
  - update-feeds

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.pip-cache"

update-rss-feeds:
  stage: update-feeds
  image: python:3.12-slim
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - if: $CI_PIPELINE_SOURCE == "web"
  cache:
    paths:
      - .pip-cache/
  before_script:
    - apt-get update && apt-get install -y git
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - |
      echo "Checking API key..."
      if [ -z "$API_KEY" ]; then
        echo "ERROR: API_KEY is not set."
        exit 1
      fi
      echo "API key is set (length: ${#API_KEY})"
    - python update_rss.py --feed-type ai-security
    - python update_rss.py --feed-type web3-security
    - |
      git config user.name "AI Security Paper Digest Bot"
      git config user.email "bot@users.noreply.github.com"
      git remote add github "https://x-access-token:${GH_PAT}@github.com/${GITHUB_REPO}.git"
      git add rss.xml web3_security_rss.xml
      git diff --cached --quiet && echo "No changes to commit" && exit 0
      git commit -m "Auto-update RSS feeds"
      git push github HEAD:main || (echo "Retrying push in 10s..." && sleep 10 && git pull --rebase github main && git push github HEAD:main)
